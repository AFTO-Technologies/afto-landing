import styles from './ProductStatement.module.scss'

export const ProductStatement = () => {
    return <div className={`${styles.productStatement}`}>
        <div className={`${styles.productDefn}`}>
        “Afto” is a mobile app that connects Event-Planners  with potential customers. Planners can use “Afto” to create and promote their Events, and connect with consumers who are interested in them. “Afto”, also provides a platform for consumers to find the best Planners for their requirements.
        </div>
    </div>
}